#include<stdio.h>

int main()
{
        int i;
        int sum;
        int n;

        scanf("%d", &n);
        sum = (1 + n) * n / 2;

        printf("The result is %d\n", sum);

        return 0;
}
